Currently the primary source of BSMP documentation is in:

http://git.cnpem.br/bruno.martins/libbsmp

Ideally this should be moved to the organization lnls-sirius at Github.com so that it can be accessed from outside the CNPEM domain.

